# CyberShield 2026: Dev / Notebook Requirements (local viva demos only)
# NOT used by Docker. Install locally with: pip install -r CyberAttackPrediction/requirements-dev.txt
# Pins notebook>=7.2 so it is compatible with jupyterlab>=4.2 (fixes the
# notebook==7.1.3 vs jupyterlab>=4.2.0 ResolutionImpossible error).
-r requirements.txt

matplotlib==3.10.0
seaborn==0.13.2
shap==0.46.0
rich==13.9.4
ipywidgets==8.1.5
notebook==7.4.7
jupyterlab==4.4.9
jupyter-server==2.16.0
tornado>=6.4.1
anyio>=4.4.0
